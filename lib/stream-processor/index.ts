/**
 * StreamProcessor 模块导出
 * @module stream-processor
 */

export { StreamProcessor } from './StreamProcessor';

export type {
  StreamProcessorConfig,
  StreamEvent,
  StreamPipeline,
  StreamSource,
  StreamTransformer,
  StreamSink,
  WindowDefinition,
  WindowResult,
  EventPattern,
  PatternCondition,
  StreamState,
  Checkpoint,
  StreamStats
} from './StreamProcessor';
